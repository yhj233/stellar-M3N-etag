#include <stdint.h>
#include "tl_common.h"
#include "main.h"
#include "epd.h"
#include "epd_spi.h"
#include "epd_bwr_296.h"
#include "drivers.h"
#include "stack/ble/ble.h"

// SSD1675 mixed with SSD1680 EPD Controller

// 原始 LUT 数组（保持原样不动，维持内存布局）
#define BWR_296_Len 50
uint8_t LUT_bwr_296_part[] = {

0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,

BWR_296_Len, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 
0x22, 0x22, 0x22, 0x22, 0x22, 0x22, 
0x00, 0x00, 0x00, 

};

#define EPD_BWR_296_test_pattern 0xA5

_attribute_ram_code_ uint8_t EPD_BWR_296_detect(void)
{
    // 原始代码，不变
    EPD_WriteCmd(0x12);
    WaitMs(10);

    EPD_WriteCmd(0x32);
    int i;
    for (i = 0; i < 153; i++)
    {
        EPD_WriteData(EPD_BWR_296_test_pattern);
    }
    EPD_WriteCmd(0x33);
    for (i = 0; i < 153; i++)
    {
        if(EPD_SPI_read() != EPD_BWR_296_test_pattern)
            return 0;
    }
    return 1;
}

_attribute_ram_code_ uint8_t EPD_BWR_296_read_temp(void)
{
    // 原始代码，不变
    uint8_t epd_temperature = 0 ;
    EPD_WriteCmd(0x12);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x74); EPD_WriteData(0x54);
    EPD_WriteCmd(0x7E); EPD_WriteData(0x3B);
    EPD_WriteCmd(0x0C); EPD_WriteData(0x8B); EPD_WriteData(0x9C); EPD_WriteData(0x96); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x01); EPD_WriteData(0x28); EPD_WriteData(0x01); EPD_WriteData(0x01);
    EPD_WriteCmd(0x11); EPD_WriteData(0x01);
    EPD_WriteCmd(0x44); EPD_WriteData(0x00); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x45); EPD_WriteData(0x27); EPD_WriteData(0x01); EPD_WriteData(0x00); EPD_WriteData(0x00);
    EPD_WriteCmd(0x3C); EPD_WriteData(0x05);
    EPD_WriteCmd(0x21); EPD_WriteData(0x00); EPD_WriteData(0x80);
    EPD_WriteCmd(0x18); EPD_WriteData(0x80);
    EPD_WriteCmd(0x22); EPD_WriteData(0xB1);
    EPD_WriteCmd(0x20);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x1B);
    epd_temperature = EPD_SPI_read();
    EPD_SPI_read();
    WaitMs(5);
    EPD_WriteCmd(0x10); EPD_WriteData(0x01);
    return epd_temperature;
}

_attribute_ram_code_ uint8_t EPD_BWR_296_Display(unsigned char *image, int size, uint8_t full_or_partial) {
    uint8_t epd_temperature = 0 ;

    // 原始初始化序列（一字不改）
    EPD_WriteCmd(0x12);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x74); EPD_WriteData(0x54);
    EPD_WriteCmd(0x7E); EPD_WriteData(0x3B);
    EPD_WriteCmd(0x0C); EPD_WriteData(0x8B); EPD_WriteData(0x9C); EPD_WriteData(0x96); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x01); EPD_WriteData(0x28); EPD_WriteData(0x01); EPD_WriteData(0x01);
    EPD_WriteCmd(0x11); EPD_WriteData(0x01);
    EPD_WriteCmd(0x44); EPD_WriteData(0x00); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x45); EPD_WriteData(0x28); EPD_WriteData(0x01); EPD_WriteData(0x00); EPD_WriteData(0x00);
    EPD_WriteCmd(0x3C); EPD_WriteData(0x05);

    // 原始全刷温度/高压序列
    EPD_WriteCmd(0x21);
    EPD_WriteData(0x00);
    EPD_WriteData(0x80);
    EPD_WriteCmd(0x18);
    EPD_WriteData(0x80);
    EPD_WriteCmd(0x22);
    EPD_WriteData(0xB1);
    EPD_WriteCmd(0x20);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x1B);
    epd_temperature = EPD_SPI_read();
    EPD_SPI_read();
    WaitMs(5);

    // 加载图像
    EPD_WriteCmd(0x4E); EPD_WriteData(0x00);
    EPD_WriteCmd(0x4F); EPD_WriteData(0x28); EPD_WriteData(0x01);
    EPD_LoadImage(image, size, 0x24);

    // 清红色 RAM
    EPD_WriteCmd(0x4E); EPD_WriteData(0x00);
    EPD_WriteCmd(0x4F); EPD_WriteData(0x28); EPD_WriteData(0x01);
    EPD_WriteCmd(0x26);
    for (int i = 0; i < size; i++) {
        EPD_WriteData(0x00);
    }

    if (!full_or_partial) {
        // ===== 局部刷新：插入高压握手，并使用正确 LUT =====
        // 局部刷新需要的前置序列（全刷已包含，局刷缺失）
        EPD_WriteCmd(0x22); EPD_WriteData(0xA1);
        EPD_WriteCmd(0x20);
        EPD_CheckStatus_inverted(100);
        EPD_WriteCmd(0x1B);
        epd_temperature = EPD_SPI_read();  // 重新读取温度（可选）
        EPD_SPI_read();
        WaitMs(5);
        EPD_WriteCmd(0x22); EPD_WriteData(0xB1);
        EPD_WriteCmd(0x20);
        EPD_CheckStatus_inverted(100);
        EPD_WriteCmd(0x21); EPD_WriteData(0x03);
        EPD_WriteCmd(0x22); EPD_WriteData(0x40);

        // 写入正确的 LUT
        uint8_t correct_lut[] = {
             0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x1E, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,                   // 增大至 0x1E 增强清除力
             0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
             0x22, 0x22, 0x22, 0x22, 0x22, 0x22, 0x00, 0x00, 0x00
    };
        EPD_WriteCmd(0x32);
        for (int i = 0; i < sizeof(correct_lut); i++) {
            EPD_WriteData(correct_lut[i]);
        }
    }

    EPD_WriteCmd(0x22); EPD_WriteData(0xC7);
    EPD_WriteCmd(0x20);
    return epd_temperature;
}

_attribute_ram_code_ uint8_t EPD_BWR_296_Display_BWR(unsigned char *image, unsigned char *red_image, int size, uint8_t full_or_partial) {
    if (red_image == NULL) {
        return EPD_BWR_296_Display(image, size, full_or_partial);
    }

    uint8_t epd_temperature = 0 ;

    // 原始初始化序列（一字不改）
    EPD_WriteCmd(0x12);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x74); EPD_WriteData(0x54);
    EPD_WriteCmd(0x7E); EPD_WriteData(0x3B);
    EPD_WriteCmd(0x0C); EPD_WriteData(0x8B); EPD_WriteData(0x9C); EPD_WriteData(0x96); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x01); EPD_WriteData(0x28); EPD_WriteData(0x01); EPD_WriteData(0x01);
    EPD_WriteCmd(0x11); EPD_WriteData(0x01);
    EPD_WriteCmd(0x44); EPD_WriteData(0x00); EPD_WriteData(0x0F);
    EPD_WriteCmd(0x45); EPD_WriteData(0x28); EPD_WriteData(0x01); EPD_WriteData(0x00); EPD_WriteData(0x00);
    EPD_WriteCmd(0x3C); EPD_WriteData(0x05);

    // 原始全刷温度/高压序列
    EPD_WriteCmd(0x21);
    EPD_WriteData(0x00);
    EPD_WriteData(0x80);
    EPD_WriteCmd(0x18);
    EPD_WriteData(0x80);
    EPD_WriteCmd(0x22);
    EPD_WriteData(0xB1);
    EPD_WriteCmd(0x20);
    EPD_CheckStatus_inverted(100);
    EPD_WriteCmd(0x1B);
    epd_temperature = EPD_SPI_read();
    EPD_SPI_read();
    WaitMs(5);

    // 加载黑白图像
    EPD_WriteCmd(0x4E); EPD_WriteData(0x00);
    EPD_WriteCmd(0x4F); EPD_WriteData(0x28); EPD_WriteData(0x01);
    EPD_LoadImage(image, size, 0x24);

    // 加载红色图像
    EPD_WriteCmd(0x4E); EPD_WriteData(0x00);
    EPD_WriteCmd(0x4F); EPD_WriteData(0x28); EPD_WriteData(0x01);
    EPD_LoadImage(red_image, size, 0x26);

    if (!full_or_partial) {
        // ===== 局部刷新：插入高压握手，并使用正确 LUT =====
        EPD_WriteCmd(0x22); EPD_WriteData(0xA1);
        EPD_WriteCmd(0x20);
        EPD_CheckStatus_inverted(100);
        EPD_WriteCmd(0x1B);
        epd_temperature = EPD_SPI_read();
        EPD_SPI_read();
        WaitMs(5);
        EPD_WriteCmd(0x22); EPD_WriteData(0xB1);
        EPD_WriteCmd(0x20);
        EPD_CheckStatus_inverted(100);
        EPD_WriteCmd(0x21); EPD_WriteData(0x03);
        EPD_WriteCmd(0x22); EPD_WriteData(0x40);

        // 写入正确的 LUT
        uint8_t correct_lut[] = {
            0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x80, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x1E, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x24, 0x22, 0x22, 0x22, 0x22, 0x22, 0x00, 0x00, 0x00
        };
        EPD_WriteCmd(0x32);
        for (int i = 0; i < sizeof(correct_lut); i++) {
            EPD_WriteData(correct_lut[i]);
        }
    }

    EPD_WriteCmd(0x22); EPD_WriteData(0xC7);
    EPD_WriteCmd(0x20);
    return epd_temperature;
}

_attribute_ram_code_ void EPD_BWR_296_set_sleep(void)
{
    EPD_WriteCmd(0x10);
    EPD_WriteData(0x01);
}